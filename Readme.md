# Step 1: Initialize and install
npm install

# Step 2: Run locally
npm run dev

# Step 3: Build for production
npm run build


After building, Vite will generate:

dist/
 ├── index.html
 ├── assets/
