version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing project dependencies...
      - npm install

  build:
    commands:
      - echo Build started on `date`
      - npm run build
      - echo Build completed on `date`

artifacts:
  files:
    - "**/*"

